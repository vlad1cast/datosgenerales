1. git commit -m "comit inicial" //para dejar un comentario en el comit.
2. git global --core.editor "'C:\VSCode\Code.exe' --wait" // para dejar un editor de texto por defecto
C:\VSCode
3. git status -s //para ver mas resumido el estado del repositorio local
4. git diff //ve las diferencias entre mis cambios locales
5. git diff --staged // ve la diferencia entre mis cambios que estan en staged
6. git checkout -b develop //para crear una rama
7. git checkout master //para cambiar a rama master